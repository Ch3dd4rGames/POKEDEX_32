# POKEDEX_32

- Convertir la primera letra del nombre de cada Pokémon en mayúsculas. Esto lo haremos en la función renderPokemonCard(). PISTA -> buscar función toUpperCase() de JS.

- Añadir un spinner (loading) de BS al #pokeCont

- Crear botones de páginas que lancen la función listPokemon.js. PISTA -> mirar en la docu de la PokeAPI las instrucciones de paginación.

- Volver a activar y que funcione IsotopeJS.